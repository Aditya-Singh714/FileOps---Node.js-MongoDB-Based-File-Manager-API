<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit - <%= filename.title %>
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 min-h-screen flex items-center justify-center px-4">
  <div class="bg-zinc-800 p-8 rounded-xl border border-zinc-700 shadow-2xl w-full max-w-xl">
    <h1 class="text-2xl font-semibold text-zinc-100 mb-6 text-center">
      Editing: <%= filename.title %>
    </h1>

    <form action="/edit" method="post" class="space-y-4">
      <!-- Hidden field to hold old filename -->
      <input type="hidden" name="old_title" value="<%= filename.title %>">

      <!-- Title Field -->
      <div>
        <label for="title" class="block text-sm text-zinc-300 mb-1">New Title</label>
        <input
          class="block w-full rounded-md px-4 py-2 bg-zinc-700 text-zinc-200 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
          type="text" name="title" id="title" value="<%= filename.title %>" required>
      </div>

      <!-- Content Field -->
      <div>
        <label for="content" class="block text-sm text-zinc-300 mb-1">Content</label>
        <textarea
          class="block w-full rounded-md px-4 py-2 bg-zinc-700 text-zinc-200 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
          name="content" id="content" rows="8" required><%- filename.content %></textarea>
      </div>

      <!-- Submit Button -->
      <div class="text-right">
        <input
          class="bg-blue-600 hover:bg-blue-700 transition px-6 py-2 rounded-md text-white font-medium cursor-pointer"
          type="submit" value="Save Changes">
      </div>
    </form>
  </div>
</body>

</html>